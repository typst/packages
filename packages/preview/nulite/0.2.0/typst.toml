[package]
name = "nulite"
version = "0.2.0"
entrypoint = "lib.typ"
authors = ["j-mueller"]
license = "MIT"
description = "Generate charts with vegalite."

repository = "https://github.com/j-mueller/typst-vegalite"

keywords = ["charts", "plot", "vegalite", "plotting"]
disciplines = ["mathematics"]
categories = ["visualization", "integration"]
compiler = "0.13.0"
exclude = ["examples"]
