[package]
name = "booktyps"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["zral0kh"]
license = "MIT"
description = "The booktabs look for tables, read off the table's own structure, with lots of configurability."
repository = "https://github.com/zral0kh/booktyps"
keywords = ["table", "booktabs", "rules", "stroke"]
categories = ["components", "layout"]
compiler = "0.13.0"
exclude = ["assets"]
