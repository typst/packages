[package]
name = "cineca"
version = "0.2.0"
entrypoint = "lib.typ"
authors = ["HPDell"]
license = "MIT"
description = "A package to create calendar with events."
repository = "https://github.com/HPdell/typst-cineca"
categories = ["layout", "visualization"]
exclude = ["test", ".gitignore"]
