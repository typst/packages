[package]
name = "sigfig"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["OverflowCat"]
license = "MIT"
description = "Typst library for rounding numbers with significant figures and measurement uncertainty."
repository = "https://github.com/OverflowCat/sigfig"
disciplines = ["engineering"]
keywords = ["numbers", "sigfig", "uncertainty"]
exclude = ["test.typ"]
