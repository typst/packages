[package]
name = "showman"
description = "Eval & show typst code outputs inline with their source"
repository = "https://github.com/ntjess/showman"
keywords = ["manual", "documentation", "eval", "inline"]
exclude = ["manual.pdf"]
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Nathan Jessurun"]
license = "Unlicense"

[tool.packager]
paths = ["lib.typ", "src/formatter.typ", "src/runner.typ", "README.md"]
