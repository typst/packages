[package]
entrypoint = "src/lib.typ"
name = "anti-matter"
version = "0.0.1"
compiler = "0.8.0"
authors = ["tinger <me@tinger.dev>"]
repository = "https://github.com/tingerrr/typst-anti-matter"
description = "Simple page numbering of front and back matter."
keywords = ["page", "numbering"]
license = "MIT"
exclude = ["example.png"]
