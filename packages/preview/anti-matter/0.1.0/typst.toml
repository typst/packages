[package]
entrypoint = "src/lib.typ"
name = "anti-matter"
version = "0.1.0"
compiler = "0.8.0"
authors = ["tinger <me@tinger.dev>"]
repository = "https://github.com/tingerrr/anti-matter"
description = "Simple page numbering of front and back matter."
keywords = ["page", "numbering", "thesis", "book"]
license = "MIT"
exclude = ["docs", "example", "README.md"]
