[package]
# mandatory field
name = "herodot" 
version = "0.2.0"
entrypoint = "lib.typ"
authors = ["gnist <https://github.com/gnist-dev>"]
license = "GPL-3.0-only"
description = "A package for making linear timelines, inspired by chronology."


# optional fields
repository = "https://github.com/gnist-dev/herodot"
categories = ["visualization"]
disciplines = ["history", "philosophy"]
exclude = ["example.pdf", "*.pdf", "example.typ", "examples/*"]
