#import "deps.typ": shorthands

#let operations = shorthands.with(
  ($@$, $compose$),
  ($*$, $dot.op$),
  ($%$, $mod$),
  ($**$, $ast.basic$),
  ($\\$, $without$),
  ($==$, $equiv$),
  ($+-$, $plus.minus$),
  ($..$, $quad$),
  ($....$, $quad quad$),
)
