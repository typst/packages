version "0.1.0"

# The same package required twice:
# - once via shortname (official URL from the package DB)
# - once via explicit fork URL
requires "asynctools"
requires "https://example.com/timotheecour/asynctools"

