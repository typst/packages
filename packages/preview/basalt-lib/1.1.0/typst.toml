[package]
name = "basalt-lib"
version = "1.1.0"
entrypoint = "src/exports.typ"
authors = ["Gabriel Talbert Bunt"]
license = "GPL-3.0-only"
description = "Note taking utilities / Zettelkasten framework."
repository = "https://github.com/GabrielDTB/basalt-lib"
keywords = [
  "notes",
  "zettelkasten",
  "knowledge-management",
]
categories = [
  "model",
  "scripting",
  "utility",
]
compiler = "0.12.0"
exclude = [
  "/release.sh",
  "/examples/",
  "/.git/",
  "/.gitignore",
  "/flake.nix",
  "/flake.lock",
]
