# 04a Developer Experience Refinement

Adress the following points:

- Add a `mise run clean` that undoes `bundle`
- Add a `mise run uninstall` that undoes `install`
- `README.md` references `plans` but it is not bundled
