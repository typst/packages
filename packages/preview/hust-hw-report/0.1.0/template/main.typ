// =====================================================================
// 华中科技大学《硬件综合训练》课程设计报告
// 由 typst init 自动生成。把下方 report.with(...) 中的信息改成自己的，
// 然后按章节撰写报告即可。编译：typst compile main.typ --font-path .
// =====================================================================
#import "@preview/hust-hw-report:0.1.0": *

#show: report.with(
  title: "5段流水CPU设计",        // 题目
  major: "计算机科学与技术",       // 专业
  class: "CS240X",               // 班级
  stu-num: "U20241224",          // 学号
  name: "郭德纲",                 // 姓名
  phone: "1345565666",
  mail: "13456@qq.com",
  year: "2026",                  // 封面年份
  course: "硬件综合训练",          // 封面蓝条左侧文字
  doc-type: "课程设计报告",        // 封面蓝条右侧文字
  declaration: true,             // 末尾自动生成"原创性声明"页
  // signature: image("assets/signature.png", height: 0.9cm),  // 手写签名（可选）
)

= 课程设计概述

== 课设目的

（在这里撰写课设目的。正文为宋体小四、两端对齐、首行缩进两字符，行距按 Word 模板的文档网格设置。）

== 设计任务

（在这里撰写设计任务。）

== 设计要求

+ 根据课程设计指导书的要求，制定出设计方案；
+ 分析指令系统格式，指令系统功能；
+ 设计出实现指令功能的硬布线控制器；
+ 调试、数据分析、验收检查；
+ 课程设计报告和总结。

== 技术指标

- 支持@tbl-inst 前 27 条基本 32 位 RISC-V 指令；
- 支持 5 段流水机制，可处理数据冒险、结构冒险、分支冒险。

#tbl(
  table(
    columns: 4,
    align: center,
    [*\#*], [*指令助记符*], [*指令类型*], [*简单功能描述*],
    [1], [ADD], [R], [加法],
    [2], [ADDI], [I], [立即数加],
    [3], [AND], [R], [与],
  ),
  caption: "设计支持的 RISC-V 指令（示例）",
  label: "tbl-inst",
)

= 总体方案设计

== 单周期 CPU 设计

本次我们采用的方案是……（此处撰写方案概要，注意本章是设计而非实现）。

总体结构图如@fig-arch 所示。

#fig("assets/demo-figure.png", caption: "总体结构图", width: 90%) <fig-arch>

=== 主要功能部件

==== 程序计数器 PC

（条目标题会自动编号为（1）（2）……使用四个等号创建。）

程序计数器 PC 的 Verilog 代码如下（代码块自动添加灰色底纹）：

```verilog
always@(negedge clk, posedge clear)
begin
    if(clear)
        pc_out<=0;
    else if(!halt)
        pc_out<=pc_in;
end
```

= 详细设计与实现

（撰写具体实现细节，尽量图文并茂。图用 `#fig(...)` 插入并用 `@fig-xx` 交叉引用，
表用 `#tbl(...)` 插入并用 `@tbl-xx` 交叉引用，编号自动按章管理。）

= 实验过程与调试

== 主要故障与调试

*故障现象：*（描述故障现象。）

*原因分析：*（分析原因。）

*解决方案：*（给出解决方案。）

= 设计总结与心得

== 课设总结

- 完成方案总结；
- 功能总结；
- 其他需要总结的内容。

== 课设心得

（撰写课设体会、收获以及对课程的建议。）

#heading(numbering: none)[参考文献]

#refs[
  DAVID A.PATTERSON(美).计算机组成与设计硬件/软件接口(原书第5版).北京：机械工业出版社.
][
  谭志虎，秦磊华，吴非，等.计算机组成原理（微课版 第2版）.北京：人民邮电出版社，2025年.
]
