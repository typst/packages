[package]
name = "spryst"
version = "0.1.0"
compiler = "0.14.0"
entrypoint = "src/lib.typ"
authors = ["TimeTravelPenguin <@TimeTravelPenguin>"]
license = "MIT"
description = "Slice a spritesheet into its individual sprites."
repository = "https://github.com/TimeTravelPenguin/spryst"
keywords = ["spritesheet", "sprite", "slicing", "image processing"]
categories = ["integration", "visualization", "utility"]
exclude = ["/assets/"]

# Tytanic test runner. `ppi` must match `PPI` in `tests/lib.typ` so that 32px
# sprite cells land on whole pixels. Comparison is exact (no per-pixel slack).
[tool.tytanic]
tests = "tests"

[tool.tytanic.default]
ppi = 300
max-delta = 0
max-deviations = 0
