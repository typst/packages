// @typstyle off
#import "kangaroo.typ": (
  secpar, secpar-unary,
  adv, lib,
  advantage, negl, poly,
  bad,
  interc, indist, linked, chain,
  bit, bits,
  algo, subr, proc,
  end-of-time,
  code, library,
  comment,
  hl,
)
