[package]
name = "itemize"
version = "0.1.0"
compiler = "0.13.1"
repository = "https://github.com/tianyi-smile/itemize"
entrypoint = "lib.typ"
authors = ["tianyi-smile"]
license = "MIT"
description = "An easy way to customize enum and list."
keywords = ["item", "list"]
categories = ["components", "utility"]
exclude = ["/assert/*", "/test/*"]
