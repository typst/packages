[package]
name = "equate"
version = "0.2.0"
entrypoint = "src/lib.typ"
compiler = "0.11.0"
authors = ["Eric Biedert"]
repository = "https://github.com/EpicEricEE/typst-equate"
license = "MIT"
description = "Breakable equations with improved numbering."
exclude = ["assets", "tests"]
categories = ["layout", "model"]
keywords = ["math", "equation", "formula", "sub", "multiline", "multi-line", "split"]
