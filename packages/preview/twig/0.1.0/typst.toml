[package]
name = "twig"
version = "0.1.0"
entrypoint = "src/lib.typ"

authors = ["QuantumRange <@QuantumRange>", "palisn <@palisn>"]
license = "GPL-3.0-only"
description = "Creates trees from lists using Cetz."

repository = "https://github.com/QuantumRange/twig"
keywords = [
    "list",
    "tree",
    "cetz"
]
categories = [
    "visualization",
]