[package]
name = "tessera"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = ["F.X.P. <https://github.com/vanleefxp>"]
license = "MIT"
repository = "https://github.com/vanleefxp/tesselate_typ"
description = "A package for combining multiple images into masonry or matrix layout."
categories = ["layout"]
keywords = ["image", "masonry", "matrix"]
