[package]
name = "neat-timetable"
version = "0.1.0"
entrypoint = "template.typ"
authors = ["Andrew Voynov"]
license = "AGPL-3.0-only"
description = "Easily create 2-week (numerator/denominator) class schedule"
repository = "https://codeberg.org/Andrew15-5/neat-timetable"
categories = ["visualization"]
keywords = [
  "schedule",
  "class",
  "pair",
  "numerator",
  "denominator",
  "university",
  "college",
  "school",
]
compiler = "0.11.1"
exclude = ["tests"]

[template]
path = "template"
entrypoint = "timetable.typ"
thumbnail = "tests/example-en/ref/1.png"
