[package]
name = "yaml-dadaism"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = ["baptiste <https://github.com/baptiste>"]
repository = "https://github.com/baptiste/yaml-dadaism"
license = "MPL-2.0"
description = "Calendar with linear monthly layout filled with yaml-defined events."
categories = ["layout", "office"]
exclude = ["assets"]
keywords = [
  "calendar", "yaml", "datetime"
]
