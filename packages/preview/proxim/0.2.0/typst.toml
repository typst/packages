[package]
name = "proxim"
version = "0.2.0"
entrypoint = "src/lib.typ"
repository = "https://github.com/Nils-TUD/typst-proxim"
authors = ["Nils Asmussen"]
license = "MIT"
description = "Labeled rectangular nodes and routed edges for drawing block diagrams."
categories = ["visualization", "components"]
keywords = [
    "diagram",
    "node",
    "edge",
    "arrow",
    "block",
    "architecture",
    "flowchart",
]
exclude = ["examples/"]
