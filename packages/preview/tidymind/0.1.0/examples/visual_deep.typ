#import "@preview/tidymind:0.1.0": mindmap, node
#set page(width: auto, height: auto, margin: 10pt)
#mindmap(node([Sistema],
  node([Camada A], node([A1], node([A1a]), node([A1b])), node([A2])),
  node([Camada B], node([B1], node([B1a]))),
))
