// 示例：总结与展望 (中文)

#let summary-section = [
  = 总结与展望

  == 工作总结

  #block(
    fill: rgb("#f1f8ff"),
    stroke: 0.5pt + rgb("#0366d6"),
    inset: 15pt,
    radius: 6pt,
  )[
    *工作一：理论与框架构建*
    #v(4pt)
    核心方案：构建了端到端自适应表征学习架构与特征聚合网络。
    #v(4pt)
    实验指标：基准评测数据集准确率达到 94.8%，达到业界领先水平。
  ]

  #v(8pt)

  #block(
    fill: rgb("#fff8f4"),
    stroke: 0.5pt + rgb("#d9730d"),
    inset: 15pt,
    radius: 6pt,
  )[
    *工作二：轻量化与工程落地*
    #v(4pt)
    核心方案：提出了多维稀疏剪枝策略与结构重参数化算法。
    #v(4pt)
    实验指标：模型参数量减少 42%，推理吞吐量提升 1.8 倍。
  ]

  == 未来展望

  + 深入探索跨领域零样本迁移泛化能力
  + 面向移动端与边缘计算设备的极致轻量化适配
  + 结合多模态大语言模型拓展下游具身智能任务
]
