// 示例：研究背景与现状 (中文)
#import "@preview/modern-szu-slides:0.1.0": *

#let background-section = [
  = 研究背景

  == 背景与动机

  此处填写研究背景与动机。支持 *粗体*、_斜体_、行内公式 $x + y = z$ 等多种排版元素。

  #card(
    [研究目标],
    [明确界定本文的研究目标与核心科学问题。使用卡片组件可以突出重要概念或核心定义。],
  )

  #pagebreak()

  == 核心挑战与问题

  针对研究目标，当前学术界与工业界面临的主要痛点与挑战：

  #v(0.5em)

  #conclusion-card(
    title: [主要科学挑战],
    [
      - *挑战一：高维异构数据建模* —— 特征空间分布稀疏，跨域对齐困难。
      - *挑战二：实时推断效率瓶颈* —— 现有方案计算复杂度过高，资源开销大。
    ],
  )
]
