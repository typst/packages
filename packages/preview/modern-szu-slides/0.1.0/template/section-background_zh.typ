// 示例：研究背景与现状 (中文)
// 可根据需要修改内容或删除此文件。

#import "slide-functions.typ": small-title, card, conclusion-card

#let background-section = [
  = 研究背景

  == 背景与动机

  此处填写研究背景与动机。支持 *粗体*、_斜体_、行内公式 $x + y = z$ 等多种排版元素。

  #card(
    [研究目标],
    [明确界定本文的研究目标与核心科学问题。使用卡片组件可以突出重要概念或核心定义。],
  )

  #pagebreak()

  == 核心挑战与问题

  #small-title[待解决的关键问题]

  - 挑战一：海量异构数据环境下的计算与存储开销过大
  - 挑战二：多模态表征空间不对齐导致的泛化性能受限
  - 挑战三：复杂动态场景中的鲁棒性与实时响应难以兼顾

  #conclusion-card(
    [核心洞察],
    [总结主要背景结论、现有方法的局限性以及本研究的技术切入点。],
  )
]
