[package]
name = "curvly"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Chad Skeeters"]
license = "MIT"
description = "Typst package for curving text on an arc or circle"
repository = "https://github.com/cskeeters/typst-curvly"
categories = ["layout"]
compiler = "0.12.0"
exclude = ["examples"]
