[package]
name = "parize"
version = "0.1.0"
compiler = "0.13.0"
repository = "https://github.com/tianyi-smile/parize"
entrypoint = "lib.typ"
authors = ["tianyi-smile"]
license = "MIT"
description = "Make block-level elements to be part of paragraphs."
keywords = ["paragraph", "indent"]
categories = ["components", "utility"]
exclude = ["asserts/", "tests/", "doc/", "examples/"]
