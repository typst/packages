[package]
name = "simple-cheatsheet"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Wasay Saeed @wasaysir"]
license = "MIT"
description = "Generate cheatsheets with automatic colour coding and sectioning easily."
categories = ["report"]

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"