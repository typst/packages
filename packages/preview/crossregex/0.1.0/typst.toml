[package]
name = "crossregex"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = ["QuadnucYard"]
license = "MIT"
description = "A crossword-like regex game written in Typst."
compiler = "0.11.0"
categories = ["fun"]
keywords = ["regex", "game", "crossword"]
repository = "https://github.com/QuadnucYard/crossregex-typ"
exclude = ["examples"]
