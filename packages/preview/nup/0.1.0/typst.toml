[package]
name = "nup"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = ["baptiste <https://github.com/baptiste>"]
repository = "https://github.com/baptiste/nup"
license = "MPL-2.0"
description = "Preview multipage documents with a defined grid layout."
categories = ["layout", "office"]
exclude = ["assets"]
keywords = [
  "multipage", "preview", "layout", "grid"
]
