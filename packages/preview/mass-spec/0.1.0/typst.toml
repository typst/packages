[package]
name = "mass-spec"
version = "0.1.0"
entrypoint = "mass-spec.typ"
authors = ["James (Fuzzy) Swift"]
license = "MIT"
description = "Mass spectra with annotations for typst."
exclude = ["/assets/*"]
