[package]
name = "latedef"
description = "Use now, define later"
authors = ["T0mstone <realt0mstone@gmail.com>"]
repository = "https://codeberg.org/T0mstone/typst-latedef"
license = "MIT-0"

version = "0.1.0"
compiler = "0.11.0"

entrypoint = "latedef.typ"
exclude = ["example-images/*.png"]