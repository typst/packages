# for a description of available keys, see https://github.com/typst/packages/?tab=readme-ov-file#package-format

[package]
name = "drawmatrix"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = [
  "Jonas Schulze <https://github.com/jonas-schulze>",
]
license = "MIT"
description = "Visualize matrices inside of equations. Inspired by the LaTeX package of the same name."
# homepage = ""
repository = "https://github.com/jonas-schulze/drawmatrix-typst"
keywords = ["draw", "matrix"]
categories = ["visualization"]
# disciplines = []
# compiler = ""
exclude = [
  ".github",
  "docs",
  "scripts",
  "tests",
  ".typstignore",
  "Justfile",
  "thumbnail-dark.svg",
  "thumbnail-light.svg",
]

# [template]
# path = "template"
# entrypoint = "main.typ"
# thumbnail = "thumbnail.png"

# [tool.mytool]
# foo = "bar"
