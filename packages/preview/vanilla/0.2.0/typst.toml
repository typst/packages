[package]
name = "vanilla"
version = "0.2.0"
entrypoint = "src/lib.typ"
repository = "https://github.com/juristack/typst-vanilla"
authors = ["David Conrad"]
license = "MIT"
description = "A vanilla Typst package. Aims for minimilist styling fimilar to classic MS Word defaults."
compiler = "0.14.0"
categories = ["paper"]
exclude = ["examples"]

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"
