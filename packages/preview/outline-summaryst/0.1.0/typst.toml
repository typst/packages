[package]
name = "outline-summaryst"
version = "0.1.0"
repository = "https://github.com/aarneng/Outline-Summary"
entrypoint = "src/outline-summaryst.typ"
authors = ["@aarneng"]
license = "MIT"
description = "A basic template for including a summary for each entry in the table of contents. Useful for writing books."
keywords = ["summary", "outline", "table of contents", "book", "novel"]
categories = ["layout"]
disciplines = ["literature"]
exclude = [".gitignore"]

