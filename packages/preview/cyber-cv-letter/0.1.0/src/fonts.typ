// font-chrome / font-body defaults. Weight-family resolution for the IBM
// Plex Medium/SemiBold quirk lives in theme.typ (resolve-font) — this file
// only owns which family names are the defaults.

#let default-font-chrome = "IBM Plex Mono"
#let default-font-body = "IBM Plex Sans"
