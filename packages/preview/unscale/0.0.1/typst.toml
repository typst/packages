[package]
name = "unscale"
version = "0.0.1"
compiler = "0.13.0"
repository = "https://github.com/davystrong/unscale"
entrypoint = "src/lib.typ"
authors = [
  "David Armstrong <https://github.com/davystrong>"
]
license = "MIT"
description = "Undo scaling inside a scale box"
categories = [ "layout" ]
keywords = [ "scale" ]
exclude = [ "/gallery/*"]
