// ============================================================================
// 设计 token —— 全局外观的唯一来源
// 改这里 = 改全局。所有颜色 / 字体 / 字号 / 间距集中在此，组件里不写死。
// ============================================================================

// ---------------------------------------------------------------------------
// 配色 —— 蓝 + 金 端庄风
// ---------------------------------------------------------------------------
#let color = (
  // 主色（深蓝，稳重）
  primary: rgb(47, 85, 151),
  primary-light: rgb(75, 115, 185),
  primary-dark: rgb(30, 55, 100),
  // 点缀（金，典雅）
  gold: rgb(184, 134, 11),
  gold-light: rgb(218, 165, 32),
  // 中性
  text: rgb(51, 51, 51), // 正文（深灰，比纯黑柔和）
  muted: rgb(120, 120, 120), // 次要文字 / 页脚
  gray-light: rgb(128, 128, 128),
  gray-dark: rgb(64, 64, 64),
  bg: white, // 页面底色
  // 莫兰迪块色（用于 5 种内容块）
  block-note: rgb(100, 110, 125), // 通用 —— 莫兰迪蓝灰
  block-definition: rgb(160, 145, 130), // 定义 —— 莫兰迪米褐
  block-alert: rgb(145, 110, 100), // 警告 —— 莫兰迪红褐
  block-example: rgb(110, 130, 125), // 示例 —— 莫兰迪青绿
  block-theorem: rgb(125, 115, 130), // 定理 —— 莫兰迪紫灰
  block-highlight: rgb(170, 160, 140), // 重点 —— 莫兰迪黄褐
)

// ---------------------------------------------------------------------------
// 字体 —— 系统已装 Noto CJK
// ---------------------------------------------------------------------------
#let font = (
  sans: ("Noto Sans CJK SC",), // 黑体：标题 / 强调
  serif: ("Noto Serif CJK SC",), // 宋体：正文
  mono: ("Noto Sans Mono CJK SC",), // 等宽：代码
)

// ---------------------------------------------------------------------------
// 字号 —— 16:9 演示页偏大
// ---------------------------------------------------------------------------
#let size = (
  title: 34pt, // 封面主标题
  subtitle: 18pt, // 封面副标题
  section: 30pt, // 章节分隔页标题
  section-num: 64pt, // 章节分隔页大号数字
  frametitle: 26pt, // 内容页标题
  body: 21pt, // 正文
  small: 16pt, // 小字 / 块标题
  footer: 12pt, // 页脚
)

// ---------------------------------------------------------------------------
// 间距 / 线条
// ---------------------------------------------------------------------------
#let layout = (
  margin: (x: 2.2em, top: 2.0em, bottom: 1.6em),
  leading: 0.85em, // 行距
  par-spacing: 1.1em, // 段距
  list-spacing: 0.9em, // 列表项间距
  rule-thin: 0.6pt, // 细线（页脚）
  rule-bold: 1.4pt, // 粗线（标题下划线）
  block-radius: 4pt, // 内容块圆角
)
