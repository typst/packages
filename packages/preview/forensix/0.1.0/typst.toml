[package]
name = "forensix"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["teismar"]
license = "MIT"
description = "Render hex dumps, MACB timelines, IOC tables, and TTP references for forensics reports."
keywords = ["hexdump", "forensics", "malware", "dfir", "incident-response", "mitre-attack"]
categories = ["visualization", "components", "report"]
disciplines = ["computer-science"]
repository = "https://github.com/teismar/typst-forensix"
homepage = "https://teismar.github.io/typst-forensix/"
exclude = [
  "/docs/*",
  "/test/*",
  "*.yml",
  "*.png",
]
