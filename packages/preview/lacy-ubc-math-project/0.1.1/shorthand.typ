// Block style limit notation.
#let limm = $limits("lim")$ 

// Block style sum notation.
#let summ = $limits(sum)$

// A horizontal rule.
#let hrule = {
  v(0.2em)
  line(length: 100%, stroke: 0.4pt)
}

