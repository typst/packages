// Block style limit notation.
#let limm = {
  $limits("lim")$
}

// Short name for the infinity symbol.
#let inf = $infinity$

// A horizontal rule.
#let hrule = {
  v(0.2em)
  line(length: 100%, stroke: 0.4pt)
}

