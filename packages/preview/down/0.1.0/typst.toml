[package]
name = "down"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Toto <the@unpopular.me>"]
license = "MIT"
description = "Pass down arguments of `sum`, `integral`, etc. to the next line, which can generate shorthand to present reusable segments."
repository = "https://git.sr.ht/~toto/down"
keywords = ["calculus"]
categories = ["utility"]
disciplines = ["mathematics"]
compiler = "0.11.0"
exclude = ["sample.pdf", "sample.typ", "README.typ"]
