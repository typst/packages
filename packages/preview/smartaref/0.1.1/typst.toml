[package]
name = "smartaref"
version = "0.1.1"
entrypoint = "src/lib.typ"
authors = ["Robin Eklind"]
license = "0BSD"
description = "Smart handling of consecutive references (i.e. cleveref for Typst)."
repository = "https://github.com/mewmew/smartaref-typ"
compiler = "0.15.0"
exclude = ["/example"]
