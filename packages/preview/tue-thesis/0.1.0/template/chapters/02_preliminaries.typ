#import "../template/prelude.typ": *

= Preliminaries <chap:preliminaries>

A #gls("hmm") #index("hidden Markov model") is expanded on first use;
later uses like #gls("hmm") and the plural #glspl("pn")
#index("Petri net") stay short. Delete these examples.

#lorem(120)

#figure(
  table(
    columns: 3,
    table.header([Method], [Precision], [Recall]),
    [Ours], [0.92], [0.88],
    [Baseline], [0.85], [0.80],
  ),
  caption: [An example table — replace me.],
) <example-table>

Equations are numbered per chapter:

$ E = m c^2 $ <mass-energy>

See @tbl:example-table and @eqt:mass-energy.

== Notation

#lorem(80)
