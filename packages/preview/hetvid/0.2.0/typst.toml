[package]
name = "hetvid"
version = "0.2.0"
entrypoint = "hetvid.typ"
authors = ["itpyi"]
license = "MIT-0"
description = "A template for light notes"
categories = ["report"]

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"
