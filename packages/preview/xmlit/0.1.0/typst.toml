[package]
name = "xmlit"
version = "0.1.0"
compiler = "0.15.0"
repository = "https://github.com/siefkenj/typst-xmlit"
entrypoint = "src/lib.typ"
authors = ["Jason Siefken <https://github.com/siefkenj>"]
categories = ["utility"]
license = "MIT"
description = "Generate XML documents using Typst syntax"
keywords = ["xml", "serialization", "markup"]
exclude = [
  "tests",
  "plugin",
  "src/elem/elem.test.typ",
  "src/relaxng/relaxng.test.typ",
]

[tool.tytanic]
tests = "tests"
