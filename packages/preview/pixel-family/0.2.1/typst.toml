[package]
name = "pixel-family"
version = "0.2.1"
entrypoint = "lib.typ"
authors = ["GiggleLiu <@GiggleLiu>"]
license = "MIT"
description = "Inline pixel art characters for Typst — drop them into text like emoji"
repository = "https://github.com/GiggleLiu/pixel-family"
keywords = ["pixel", "character", "avatar", "inline"]
categories = ["components"]
compiler = "0.13.1"
exclude = ["manual.*", "Makefile", "CLAUDE.md", ".github/*", "dist/*", "docs/*"]
