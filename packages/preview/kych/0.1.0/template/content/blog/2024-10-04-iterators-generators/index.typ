// =============================================================================
// 博客文章：Python 中迭代器与生成器的对比
// =============================================================================
// 这篇示例博客展示了 跨越晨昏 模板中以下特性的使用：
// 1. 脚注 (footnote) → 自动转换为侧注 (marginnote)
// 2. 代码块 (code block) → 语法高亮显示 Python 代码
// 3. 图表 (figure) → 带侧注标题的插图
// 4. markdown 启用的自动链接
// =============================================================================

// 导入模板（从 blog/index.typ 导入，链式导入到 template）
#import "../index.typ": template, kych
#show: template

// 文章标题
= Python 中的迭代器与生成器

// 引言段落，包含第一个脚注 → 自动渲染为右侧侧注
Python 的迭代协议是编写高效、地道的 Python 代码的基础。#footnote[迭代器协议于 2001 年在 Python 2.2 中作为 PEP 234 引入。]虽然迭代器和生成器密切相关，但理解它们的区别有助于你选择合适的工具完成工作。

// --- 第一节：什么是迭代器 ---
== 什么是迭代器

// 解释迭代器协议，第二个脚注提供可迭代对象 vs 迭代器的区别
迭代器是实现迭代器协议的任何对象：即 `__iter__()` 和 `__next__()` 方法。#footnote[_可迭代对象_ 在调用 `__iter__()` 时返回一个迭代器，而 _迭代器_ 自身在调用 `__iter__()` 时返回自身，并通过 `__next__()` 产生值。]当你遍历列表或元组时，Python 在幕后创建一个迭代器。你也可以通过在类中定义这些方法来构建自定义迭代器。


// Python 代码块：手动实现 Counter 迭代器类
```python
class Counter:
    def __init__(self, max):
        self.max = max
        self.current = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.current >= self.max:
            raise StopIteration
        self.current += 1
        return self.current
```

// --- 第二节：生成器登场 ---
== 生成器登场

// 生成器通过 yield 关键字简化迭代器创建
生成器是一种更简单的方式来创建迭代器，通过函数和 `yield` 关键字。#footnote[Python 还支持生成器表达式，如 `(x*2 for x in range(10))`，类似列表推导式但创建生成器而非列表。]生成器无需在类属性中维护状态，而是在调用之间自动保存状态。这使得它们更简洁，通常也更易读。


// Python 代码块：用生成器函数实现相同功能
```python
def counter(max):
    current = 0
    while current < max:
        current += 1
        yield current
```

// 对比：生成器函数生成的代码量远少于迭代器类
生成器函数产生的结果与迭代器类相同，但样板代码大大减少。#footnote[生成器在局部变量和执行位置中保存状态，使其本质上成为可恢复的函数。]当你调用生成器函数时，它返回一个自动实现迭代器协议的生成器对象。

// --- 第三节：内存效率 ---
== 内存效率

// 迭代器和生成器都是惰性求值 (lazy evaluation)
迭代器和生成器都是惰性的——它们按需生成值，而不是将所有值存储在内存中。这使得它们非常适合处理大型数据集或无限序列。#footnote[`itertools` 模块提供了强大的函数，如 `count()` 和 `cycle()`，用于创建无限迭代器。]生成十亿个数字的生成器消耗的内存极少，而十亿个数字的列表会耗尽大多数系统的内存。


// 插图：图标题自动渲染为侧注（marginnote 格式）
#figure(image("imgs/python.webp"), caption: [四人手持一条长蟒蛇])

// --- 第四节：何时使用哪种工具 ---
== 何时使用哪种工具

大多数情况下使用生成器——它们更简单、更易读。当你需要复杂的状态管理、多个迭代器方法，或者在构建需要更复杂行为的可复用组件时，才使用自定义迭代器类。

理解这些工具可以让你用更优雅的方式解决常见的编程挑战。

