// =============================================================================
// 博客文章：正态分布
// =============================================================================
// 这篇文章展示了 跨越晨昏 模板的多个高级特性：
// 1. 数学公式 (行内 $mu$ 和块级公式) → 使用 Tufte 风格公式渲染
// 2. 交叉引用 (@citation) → 自动渲染为编号链接
// 3. 代码块中嵌入图表 → lilaq 包绘制正态分布曲线
// 4. 参考文献 (#bibliography) → 使用 BibTeX 格式的文献引用
// 5. 脚注 → 自动转为侧注
// =============================================================================

#import "../index.typ": template, kych
// 导入 lilaq 绘图库，用于绘制正态分布曲线图
#import "@preview/lilaq:0.6.0" as lq
#show: template

// 文章标题
= 正态分布

// 引言段落，包含学术引用 → 使用 @degroot2012probability 格式
正态分布通常被称为高斯分布或钟形曲线，是统计学和概率论中最基础的概念之一 @degroot2012probability。其特有的对称钟形曲线在自然界和人类活动中随处可见，从人类身高和考试成绩到测量误差和生物变异皆是如此。

// --- 第一节：关键特性 ---
== 关键特性

// 参数说明：均值 μ 和标准差 σ
// 行内公式 $mu$ 和 $sigma$ → 在 HTML 中渲染为 <span role="math">
正态分布完全由两个参数定义：均值 ($mu$) 和标准差 ($sigma$) @rice2006mathematical。均值决定了分布的中心位置，而标准差控制其分散程度。约 68% 的值落在均值的一个标准差范围内，95% 落在两个标准差内，99.7% 落在三个标准差内——这一规则被称为经验法则或 68-95-99.7 规则。

// 概率密度函数 (PDF) 公式 — 块级公式，产生 <figure role="math">
概率密度函数如下：

// 经典的正态分布概率密度函数公式
$ f(x) = 1/(sigma sqrt(2pi)) e^(-(x-mu)^2/(2sigma^2)) $

// 公式来源引注：高斯 1809 年的天文学著作
这一公式由高斯在其天文学著作中提出 @gauss1809theoria，已成为现代统计学的基础 @stigler1982gauss。


// --- 正态分布曲线图 ---
// 使用 lilaq 绘图库 + Typst 计算功能绘制标准正态分布曲线 (μ=0, σ=1)
#{
  // 计算标准正态分布的 y 值：f(x) = e^(-x²/2) / √(2π)
  // range(-7, 8) 生成 x 坐标 -3.5 到 3.5，步长 0.5
  let diagram = html.frame(lq.diagram(
    xaxis: (subticks: none),   // x 轴无子刻度
    yaxis: (subticks: none),   // y 轴无子刻度
    lq.bar(
      range(-7, 8).map(x => x / 2.0),            // x 轴数据点
      range(-7, 8).map(x => {
        let z = x / 2.0
        calc.exp(-z * z / 2) / calc.sqrt(2 * calc.pi)  // 标准正态分布公式
      }),
      fill: blue.lighten(50%),   // 浅蓝色填充
    ),
  ))

  // 图表带侧注标题（标题自动移到右侧边距中）
  figure(caption: [正态分布], diagram)
}

// --- 第二节：为什么重要 ---
== 为什么重要

// 中心极限定理的说明
正态分布的重要性源于中心极限定理，#footnote[中心极限定理要求被求和的随机变量是独立同分布且具有有限方差。随着变量数量的增加，收敛到正态分布的效果会更好。]该定理指出：大量独立随机变量的和趋于正态分布，无论原始分布是什么。这解释了为何如此多的自然现象都遵循这一模式。

// 正态分布的四大应用领域
在实践中，正态分布可用于：
- 统计推断与假设检验
- 制造业质量控制
- 金融风险评估
- 自然测量建模

// --- 第三节：实际应用 ---
== 实际应用

科学家使用正态分布来建模从智商分数到气体中粒子速度的各种现象。工程师将其应用于可靠性分析和信号处理。金融分析师依赖正态分布进行投资组合理论和期权定价，尽管真实市场收益常常偏离正态性。#footnote[金融收益通常表现出"肥尾"（极端事件概率更高）和偏度，与正态分布相比有所不同。这对风险管理和衍生品定价具有重要意义。]

理解正态分布为统计思维和数据分析奠定基础，使其成为研究人员、分析人员和各领域决策者的必备工具。

// 参考文献列表 — 从 refs.bib 文件加载 BibTeX 格式的文献条目
#bibliography("refs.bib")

