// =============================================================================
// 文档：配置指南 (Configuration)
// =============================================================================
// 详细说明 跨越晨昏 项目的结构、API 函数、配置方式和页面继承机制。
// =============================================================================

#import "../index.typ": template, kych
#show: template.with(title: "配置指南")

// --- 项目结构 ---
= 网站结构

// 四个核心组成部分
模板有 4 个主要组成部分：

- `config.typ` —— 核心布局配置
- `content/` —— 存储所有网站内容
- `assets/` —— 存储共享静态资源，如全局 CSS
- `Makefile` —— 用于构建网站

// --- API 参考 ---
== API 参考

// 目前实现的功能列表
目前实现了三个函数：

- `kych-web` —— 主模板函数
- `margin-note` —— 将内容放入侧注（有限支持）
- `full-width` —— 将内容放入全宽容器（有限支持）

// --- 主配置 ---
== 主配置

// 在 config.typ 中通过 .with() 方法预设模板参数
在 `config.typ` 中，通过自定义包中的 `kych-web` 模板来定义你自己的模板。你可以在此处自定义顶部导航链接和网站标题：

```typst
#import "@preview/kych:0.1.1"

#let template = kych.kych-web.with(
  header-links: (
    "/": "首页",
    "/docs/": "文档",
    "/blog/": "博客",
    "/cv/": "简历",
    "/about/": "关于",
  ),
  title: "跨越晨昏",
)
```

// --- 层级与继承 ---
== 层级与继承

// 页面继承链说明：根页面导入 config.typ，子页面导入父级 index.typ
网站是层级结构的。根页面从 `../config.typ` 导入，而子页面从其父级的 `../index.typ` 文件导入，实现继承——无需从更上层重复导入。

// 所有 content/ 目录下的 **\/index.typ 文件自动成为可通过文件夹路径访问的页面
`content/` 中所有的 `**/index.typ` 文件都会自动成为可通过其文件夹路径访问的页面（例如 `content/posts/index.typ` → `example.com/posts`）。使用 `#link("relative/path/")[点击这里]` 链接到页面。

// 在任何层级修改定义，子页面自动继承变更
你可以在任何层级修改定义，子页面将继承这些变更。例如，要更改页面标题，从父级导入所有定义并修改 `template`：

```typst
#import "../index.typ": *
#show: template.with(title: "新标题")
```

