// =============================================================================
// 文档：样式自定义 (Styling)
// =============================================================================
// 说明如何通过 CSS 自定义 跨越晨昏 网站的外观。
// 包括默认样式表、自定义样式、完全覆盖样式表等方法。
// =============================================================================

#import "../index.typ": template, kych
#show: template.with(title: "样式自定义")

= 样式自定义

// 网站外观完全由 CSS 控制
网站的外观由 CSS 控制。

// --- 默认样式表 ---
== 默认样式表
// css 参数接受样式表 URL/路径数组
// 默认加载三个样式表（按优先级从低到高）
模板接受一个 `css` 参数，该参数接受一个样式表 URL 或路径的数组。默认情况下，它加载三个样式表：

```typst
#let kych-web(
  // ...
  css: (
    "https://cdnjs.cloudflare.com/ajax/libs/tufte-css/1.8.0/tufte.min.css",  // Tufte CSS 框架
    "/assets/kych.css",       // 跨越晨昏 项目的补充样式
    "/assets/custom.css",     // 用户自定义样式（覆盖前两者）
  ),
  // ...
)
```

// --- 自定义样式 ---
== 自定义样式
// 修改 assets/custom.css 即可，因为它最后加载，优先级最高
要自定义网站的外观，只需修改 `assets/custom.css`。由于它默认最后加载，你的规则将覆盖其他规则。

// 例如修改链接颜色为红色
例如，要更改链接颜色：

```css
a {
  color: #ff0000;
}
```

// --- 完全覆盖样式表 ---
== 覆盖样式表
// 在 config.typ 中提供自己的样式表列表，完全替换默认值
要覆盖默认样式表，你可以在 `config.typ` 中提供自己的样式表列表。例如，只使用自定义样式表：

```typst
#import "@preview/kych:0.1.1"

#let template = kych.kych-web.with(
  css: ("/assets/style.css",),  // 只加载自定义样式表
)
```

