// =============================================================================
// 文档：快速入门 (Quick Start)
// =============================================================================
// 指导用户如何安装和构建 跨越晨昏 网站项目。
// =============================================================================

#import "../index.typ": template, kych
// 覆盖页面标题为 "快速入门"
#show: template.with(title: "快速入门")

= 快速入门

// --- 安装 ---
== 安装

// 使用 typst init 命令初始化项目
要使用 `kych` 模板初始化新项目，在终端中运行以下命令：

```sh
typst init @preview/kych:0.1.1 my-website
cd my-website
```

// --- 构建 ---
== 构建

// Makefile 自动化构建流程
模板包含一个 `Makefile` 用于自动化构建流程。要构建网站，运行：

```sh
make html
```

// make html 的工作流程说明
该命令将 `content/` 目录中的所有 `.typ` 文件编译为 `_site/` 目录中的 HTML 文件。

