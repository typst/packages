#import "../i18n.typ": *

#chapter-lt("ch-discussion")

#body-lt("discussion-p1")

// Acronym examples (see acronyms.typ): first use expands, later uses stay short.
This thesis was prepared at @ucy as an @ade. Studies may involve @var systems;
health metrics such as @bmi are sometimes reported alongside @adhd findings.
Software prototypes often expose an @api:pl to external tools.

#section-lt("sec-generic")
#section-lt("sec-generic")
#section-lt("sec-generic")
