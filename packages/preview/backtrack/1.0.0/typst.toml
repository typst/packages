[package]
name = "backtrack"
version = "1.0.0"
entrypoint = "src/lib.typ"
authors = ["TheLukeGuy"]
license = "Apache-2.0"
description = "Check the current compiler version on any version."
repository = "https://github.com/TheLukeGuy/backtrack"
keywords = ["version", "compiler", "backward", "compatibility", "old", "sys"]
