[package]
name = "mitex"
version = "0.2.7"
entrypoint = "lib.typ"
authors = ["Myriad-Dreamin", "OrangeX4", "Enter-tainer", "Shichien"]
license = "Apache-2.0"
description = "LaTeX support for Typst, powered by Rust and WASM."

repository = "https://github.com/mitex-rs/mitex"
categories = ["utility"]
keywords = ["wasm", "rust", "LaTeX", "equation"]
exclude = ["examples"]