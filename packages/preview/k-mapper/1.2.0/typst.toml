[package]
name = "k-mapper"
version = "1.2.0"
entrypoint = "lib.typ"
authors = ["Derek Chai"]
license = "MIT"
description = "A package to add Karnaugh maps into Typst projects."
repository = "https://github.com/derekchai/typst-karnaugh-map"
categories = ["visualization"]
keywords = [
    "karnaugh", "map", "k map", "k-map", "electronics", "physics"
]
exclude = ["*.pdf", "*.png"]