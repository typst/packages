# Malo's Presets

Malo's Presets is a set of bare-bones presets that improve the default look of Typst documents.

## Features & Usage

Malo's Presets only applies set rules and show-set rules that can be easily overridden if you don't like them. Start a new project using the template to see a list of features.
