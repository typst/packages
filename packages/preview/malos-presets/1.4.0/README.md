# Malo's Presets

Malo's Presets is a set of bare-bones presets that improve the default look of Typst documents.

## Features & Usage

Malo's Presets only a few set and show rules that improve upon the default document style. Start a new project using the template, or read the thumbnail, to see a list of features.
