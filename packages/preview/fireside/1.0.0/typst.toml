[package]
name = "fireside"
description = "A simple letter template with a touch of warmth"
version = "1.0.0"
entrypoint = "lib.typ"
authors = ["Edgar Onghena <https://edgar.bzh/>"]
license = "MIT"
categories = ["office"]
keywords = ["letter", "color", "warm"]
exclude = [".git", ".demo", ".rendered"]
compiler = "0.11.0"

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"

