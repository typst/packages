[package]
name = "kleene"
# @scrybe(grep {{version}})
version = "0.1.0"
entrypoint = "src/lib.typ"

authors = ["Neven Villani <neven@crans.org>"]
description = "PEG-based parser combinator"
license = "MIT"

repository = "https://github.com/Vanille-N/kleene"
keywords = ["parser", "parsing", "peg"]
categories = ["text", "scripting"]

exclude = ["*.png"]
