[package]
name = "pastiglie"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Adriano Fantini <@adrfantini>"]
license = "MIT"
description = "Multi-line bi-colored GitLab-style scoped labels (pills)."
repository = "https://github.com/adrfantini/typst-scoped-pills"
keywords = ["label", "badge", "pill", "gitlab", "scoped-label"]
categories = ["components"]
compiler = "0.14.2"
exclude = ["manual.typ", "manual.pdf"]
