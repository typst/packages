[package]
name = "oasis-align"
version = "0.1.0"
entrypoint = "oasis-align.typ"
authors = ["@jdpieck"]
license = "MIT"
description = "Cleanly place content side by side with equal heights using automatic content sizing."
repository = "https://github.com/jdpieck/oasis-align"
keywords = ["align", "content", "grid", "wrap"]
categories = ["layout"]
exclude = ["examples"]