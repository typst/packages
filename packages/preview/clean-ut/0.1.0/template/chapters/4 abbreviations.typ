#let abbreviations()=[
  #v(0.7em)
  #align(center)[
    
    //could look something like this:
  #table(columns:2,
    table.header([Abbreviation],[Meaning]),
    [67],[nonsensical internet expression],
    [...], [...],
    
  )]
  
]