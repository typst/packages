#let std-style = (
  stroke: (paint: black),
  scale: 1,
  debug: false,

  // connections
  connection-size: 0.3,
  bezier-connection: 0.7,
  //  bridges
  bridge-space: 0.4,
  bridge-offset: 0,
  bridge-type: none,

  // translation
  transform: (),

  // canvas
  background: none,
  padding: 0pt,
  canvas-stroke: none,
)
