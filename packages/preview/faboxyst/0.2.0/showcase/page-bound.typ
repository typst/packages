#import "@preview/faboxyst:0.2.0": *
#set text(font: "DejaVu Sans", size: 11pt)

#bound-page[
  = bound-page
  Une page reliée : la spirale court sur le bord d’attaque (gauche en LTR).

  #notebook-box(title: [Exercice], badge: [01])[
    $ x^2 + y^2 = r^2 $
  ]
]

#pagebreak()
#set text(lang: "ar", dir: rtl)
#bound-page[
  = صفحة مجلّدة
  في الاتجاه العربي تنتقل الحلقة إلى اليمين.

  #notebook-box(title: [تمرين], badge: [٠٢])[
    $ a + b = c $
  ]
]
