// 字体定义
// 优先使用 Windows 预装字体，兼顾 Linux/macOS 的常见开源替代

// 宋体 (正文默认)
#let songti = ("Times New Roman", "SimSun", "STSong", "Songti SC")

// 黑体 (标题默认)
#let heiti = ("Times New Roman", "SimHei", "STHeiti", "Heiti SC")

// 纯黑体 (无 Times New Roman)
#let pure-heiti = ("SimHei", "STHeiti", "Heiti SC")

// 楷体 (强调/引用)
#let kaiti = ("Times New Roman", "KaiTi", "STKaiti", "Kaiti SC")

// 等宽字体 (代码)
#let code = ("Consolas", "SimSun", "Menlo", "Monaco")
