[package]
authors = ["Ludwig Austermann"]
compiler = "0.8.0"
description = "Package providing convenient functional functions to use on arrays."
entrypoint = "funarray.typ"
exclude = ["examples"]
license = "MIT"
name = "funarray"
repository = "https://github.com/ludwig-austermann/typst-funarray"
version = "0.3.0"
