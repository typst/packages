[package]
name = "materialize"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = ["Julian <@rojul>"]
license = "MIT"
description = "Generate Matter setup codes for smart home devices with ease."
repository = "https://github.com/rojul/typst-materialize"
categories = ["components"]
exclude = ["/examples"]
